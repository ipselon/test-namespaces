// import selectToolboxcontainer from '../selectors';
import expect from 'expect';

// const selector = selectToolboxcontainer;

describe('selectToolboxcontainer', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
